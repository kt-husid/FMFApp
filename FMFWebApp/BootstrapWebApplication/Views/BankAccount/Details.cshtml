@model BootstrapWebApplication.Models.BankAccount

@if (!@Request.IsAjaxRequest())
{
    ViewBag.Title = @BootstrapResources.Resources.Details;
    <h2>@BootstrapResources.Resources.Details</h2>
}
<div>
    @if(!@Request.IsAjaxRequest())
    {
        <h4>@Resources.BankAccount</h4>
        <hr />
    }
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.IsPrimary)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.IsPrimary)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.RegNumber)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.RegNumber)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.AccountNumber)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.AccountNumber)
        </dd>

    </dl>
</div>
@if (!@Request.IsAjaxRequest())
{
    @Html.Partial("_EditBackToListPartial", Model)
}
<script>
    $(function () {
        $(".detailsModal").click(function (e) {
            window.location = '/@ViewContext.GetControllerName()/Details/@Model.Id';
        });
    });
</script>