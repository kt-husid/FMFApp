@model BootstrapWebApplication.Models.Person

@Html.Partial("_DeleteHeaderPartial", new BootstrapCore.Helpers.FormDeleteData() { ViewTitle = @Resources.Person })
@if (!@Request.IsAjaxRequest())
{
    // display any information to the user before deleting?
}
@Html.Partial("_DeleteFooterPartial", new BootstrapCore.Helpers.FormDeleteData() { Id = @Model.Id })