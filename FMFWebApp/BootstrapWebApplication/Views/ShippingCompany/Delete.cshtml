@model BootstrapWebApplication.Models.ShippingCompany

@Html.Partial("_DeleteHeaderPartial", new BootstrapCore.Helpers.FormDeleteData() { ViewTitle = @Resources.ShippingCompany })
@if (!@Request.IsAjaxRequest())
{
    // display any information to the user before deleting?
}
@Html.Partial("_DeleteFooterPartial", new BootstrapCore.Helpers.FormDeleteData() { Id = @Model.Id })